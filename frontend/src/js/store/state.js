/**
 * @name event
 * @param {int} x
 * @param {int} y
 * @param {function} func
 */

const initialState = {
  number: 0,
  mouse: {
    x: 0,
    y: 0
  },
  events: []
};

export default initialState;
